{
  "name": "@kb/shared",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo \"shared package has no build step\"",
    "lint": "echo \"shared package has no lint step\"",
    "test": "echo \"shared package has no test step\""
  }
}
