{
  "name": "@kb/api",
  "private": true,
  "scripts": {
    "setup": "node ../../scripts/run.mjs setup-venv",
    "dev": "node ../../scripts/run.mjs venv --cwd apps/api uvicorn src.main:app --reload --host 0.0.0.0 --port @{API_PORT:8000}",
    "lint": "node ../../scripts/run.mjs venv --cwd apps/api python -m compileall src scripts",
    "build": "node ../../scripts/run.mjs venv --cwd apps/api python -m compileall src scripts",
    "test": "node ../../scripts/run.mjs venv --cwd apps/api python -m unittest discover tests"
  }
}
